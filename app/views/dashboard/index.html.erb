<div class="row">
  <div class="medium-6 columns">
    <div class="callout">
      <table>
        <tr>
          <td>Gateway IP</td>
          <td></td>
        </tr>
        <% if @server.present? %>
        <tr>
          <td>Proxy Server</td>
          <td><%= @server.name %></td>
        </tr>
        <tr>
          <td>Proxy IP</td>
          <td><%= @server.host %></td>
        </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>
